## What this changes

## Why

## Checklist
- [ ] I ran `npm test` and it passes
- [ ] I tested the change in the running app (Todo.app or `./start.sh`)
- [ ] No `innerHTML` with task/list-derived strings; the server still binds `127.0.0.1` only
- [ ] If I touched date / quick-add / summary logic, I added or updated tests
